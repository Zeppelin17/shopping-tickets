services:
  web:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - .:/data

  web_migrate:
    extends:
      service: web
    command: python manage.py migrate

  web_run:
    extends:
      service: web
    command: python manage.py runserver
    ports:
      - "8000:8000"